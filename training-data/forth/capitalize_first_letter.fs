VARIABLE SRC-ADDR
VARIABLE SRC-LEN

: CAPITALIZE ( addr len -- )
  SRC-LEN !
  SRC-ADDR !
  SRC-ADDR @ C@ DUP [CHAR] a [CHAR] z 1+ WITHIN
  IF 32 - THEN
  EMIT
  SRC-ADDR @ 1+ SRC-LEN @ 1- TYPE ;

S" hello" CAPITALIZE CR
S" world" CAPITALIZE CR
