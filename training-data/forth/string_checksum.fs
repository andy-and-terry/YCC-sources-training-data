VARIABLE SRC-ADDR
VARIABLE SRC-LEN
VARIABLE CHECKSUM

: STRING-CHECKSUM ( addr len -- sum )
  SRC-LEN !
  SRC-ADDR !
  0 CHECKSUM !
  SRC-LEN @ 0 DO
    SRC-ADDR @ I + C@ CHECKSUM +!
  LOOP
  CHECKSUM @ ;

S" hello" STRING-CHECKSUM .
S" world" STRING-CHECKSUM .
CR
