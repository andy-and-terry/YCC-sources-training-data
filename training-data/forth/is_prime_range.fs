: IS-PRIME? ( n -- flag )
  DUP 2 < IF DROP FALSE EXIT THEN
  DUP 2 =  IF DROP TRUE EXIT THEN
  DUP 2 MOD 0= IF DROP FALSE EXIT THEN
  TRUE SWAP
  3
  BEGIN
    2DUP DUP * <=
  WHILE
    2DUP MOD 0= IF SWAP DROP FALSE SWAP THEN
    2 +
  REPEAT
  2DROP ;

: PRIMES-IN-RANGE ( limit -- )
  1+ 2 DO
    I IS-PRIME? IF I . THEN
  LOOP ;

30 PRIMES-IN-RANGE
CR
