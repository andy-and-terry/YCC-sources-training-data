VARIABLE CUR-MIN
VARIABLE CUR-MAX

: INIT-MINMAX ( n -- )
  DUP CUR-MIN !
  CUR-MAX ! ;

: UPDATE-MINMAX ( n -- )
  DUP CUR-MIN @ < IF DUP CUR-MIN ! THEN
  DUP CUR-MAX @ > IF CUR-MAX ! ELSE DROP THEN ;

: PRINT-MINMAX ( -- )
  ." Min: " CUR-MIN @ . ." Max: " CUR-MAX @ . CR ;

7 INIT-MINMAX
3 UPDATE-MINMAX
9 UPDATE-MINMAX
5 UPDATE-MINMAX
PRINT-MINMAX
